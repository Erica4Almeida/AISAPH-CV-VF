apiVersion: v1
kind: Secret
metadata:
  name: frontend-secret
  namespace: aisaph
type: Opaque
# Replace with your actual Strapi API token (created in the Strapi admin panel).
stringData:
  STRAPI_API_TOKEN: "CHANGE_ME"
