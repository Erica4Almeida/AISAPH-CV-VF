apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: production
type: Opaque
# stringData accepts plain text — Kubernetes encodes to base64 automatically.
# Replace POSTGRES_PASSWORD before applying.
stringData:
  POSTGRES_USER: "aisaph"
  POSTGRES_PASSWORD: "Q0hBTkdFX01F"
  POSTGRES_DB: "aisaph"
